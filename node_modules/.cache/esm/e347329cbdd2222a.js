let express,connectDatabase,cors;_bdd‍.w("express",[["default",["express"],function(v){express=v}]]);_bdd‍.w("./config/db",[["default",["connectDatabase"],function(v){connectDatabase=v}]]);_bdd‍.w("cors",[["default",["cors"],function(v){cors=v}]]);



//express init
const app = express();

//connects database
connectDatabase();

app.use(express.json({extended: false}));
app.use("/", require("./routes/noteRoute"));

app.use(cors({
    origin: "http://localhost:3000"
}));

app.get("/", (req, res) =>{
    res.send("http get request sent to root end point");
});



const port = 5000;
app.listen(port, () => console.log(`express server running on ${port}`));