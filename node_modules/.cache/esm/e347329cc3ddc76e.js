let express,connectDatabase,cors;_c3d‍.w("express",[["default",["express"],function(v){express=v}]]);_c3d‍.w("./config/db",[["default",["connectDatabase"],function(v){connectDatabase=v}]]);_c3d‍.w("cors",[["default",["cors"],function(v){cors=v}]]);


const Note = require("./models/noteModel");

//express init
const app = express();
app.use(cors());

//connects database
connectDatabase();

//middleware
app.use(express.json({extended: false}));
app.use("/", require("./routes/noteRoute"));

app.get("/", (req, res) =>{
    res.send("http get request sent to root end point from port 5000");
});


const port = 5000;
app.listen(port, () => console.log(`express server running on ${port}`));